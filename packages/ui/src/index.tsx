import type {ButtonHTMLAttributes,PropsWithChildren} from "react";export function Button(p:ButtonHTMLAttributes<HTMLButtonElement>){return <button {...p} className={`button ${p.className??""}`}/>}export function Card({children}:PropsWithChildren){return <section className="card">{children}</section>}export function NumberBall({value,selected=false}:{value:number;selected?:boolean}){return <span className={`ball ${selected?"selected":""}`} aria-label={`Dezena ${value}`}>{String(value).padStart(2,"0")}</span>}
