"use client";import{useState}from"react";import{Button,Card,NumberBall}from"@atlas/ui";export default function Generate(){const[games,setGames]=useState<number[][]>([]),[busy,setBusy]=useState(false);async function run(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget),lottery=String(f.get("lottery")),count=Number(f.get("count"));try{const r=await fetch("/api/games/generate",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({lottery,count,seed:crypto.randomUUID()})});const d=await r.json();setGames(d.games?.map((g:{numbers:number[]})=>g.numbers)??[])}finally{setBusy(false)}}return<><p className="eyebrow">Gerar jogos</p><h1>Sugestões reproduzíveis</h1><div className="hero"><Card><form onSubmit={run}><label>Modalidade<select name="lottery"><option value="mega-sena">Mega-Sena</option><option value="lotofacil">Lotofácil</option><option value="dia-de-sorte">Dia de Sorte</option></select></label><label>Quantidade<input name="count" type="number" min="1" max="20" defaultValue="3"/></label><Button disabled={busy}>{busy?"Gerando…":"Gerar amostra uniforme"}</Button></form></Card><Card><b>Transparência</b><p className="muted">Amostragem uniforme sem reposição. Cada combinação simples tem a mesma probabilidade teórica; este gerador não prevê resultados.</p></Card></div>{games.length>0&&<Card><h2>Novo lote</h2>{games.map((g,i)=><div key={i}>{g.map(n=><NumberBall key={n} value={n}/>)}</div>)}</Card>}</>}