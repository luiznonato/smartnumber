export const PIPELINE=["collect","validate","persist-and-outbox","evaluate-games","update-features","create-snapshot","generate-suggestions","atomic-publish","notify"] as const;export function jobKey(lottery:string,contest:number,revision:number,version:string,stage:string){return`${lottery}:${contest}:${revision}:${version}:${stage}`}export function assertOrdered(cutoff:number,target:number){if(target!==cutoff+1)throw new Error(`Out-of-order draw: expected ${cutoff+1}, got ${target}`)}