import{describe,it,expect}from"vitest";import{parseAdminJson,payloadHash,DisabledCaixaProvider}from"./ingestion.js";describe("ingestion",()=>{const row={lottery:"mega-sena",contestNumber:1,drawDate:"1996-03-11",numbers:[4,5,30,33,41,52]};it("previews valid official JSON",()=>expect(parseAdminJson(JSON.stringify(row),"https://loterias.caixa.gov.br/arquivo.json")[0].status).toBe("accepted"));it("quarantines invalid payload",()=>expect(parseAdminJson(JSON.stringify({...row,numbers:[1,1]}),"https://loterias.caixa.gov.br/arquivo.json")[0].status).toBe("rejected"));it("hashes deterministically",()=>expect(payloadHash(row)).toBe(payloadHash(row)));it("does not invent an endpoint",async()=>await expect(new DisabledCaixaProvider().latest("mega-sena")).rejects.toThrow("desativada"))});