import "reflect-metadata";import {NestFactory} from "@nestjs/core";import {SwaggerModule,DocumentBuilder} from"@nestjs/swagger";import{FastifyAdapter,NestFastifyApplication}from"@nestjs/platform-fastify";import helmet from"@fastify/helmet";import cookie from"@fastify/cookie";import {AppModule} from"./app.module.js";
async function bootstrap(){const app=await NestFactory.create<NestFastifyApplication>(AppModule,new FastifyAdapter({trustProxy:true}));await app.register(helmet);await app.register(cookie);app.setGlobalPrefix("api");app.enableCors({origin:process.env.WEB_URL??"http://localhost:3000",credentials:true});const config=new DocumentBuilder().setTitle(process.env.APP_NAME??"Atlas Loto API").setDescription("API de análise estatística; scores não são probabilidades de prêmio.").setVersion("0.1.0").addCookieAuth("atlas_session").build();SwaggerModule.setup("docs",app,SwaggerModule.createDocument(app,config));await app.listen({port:Number(process.env.PORT??3001),host:"0.0.0.0"});}void bootstrap();