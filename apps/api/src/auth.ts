import{hash,verify}from"argon2";import{createHash,randomBytes,timingSafeEqual,createHmac}from"node:crypto";export async function hashPassword(password:string){if(password.length<12)throw new Error("Senha deve ter pelo menos 12 caracteres");return hash(password,{type:2,memoryCost:19456,timeCost:2,parallelism:1})}export async function verifyPassword(encoded:string,password:string){return verify(encoded,password)}export function issueSession(){const token=randomBytes(32).toString("base64url");return{token,tokenHash:createHash("sha256").update(token).digest("hex")}}export function verifyWebhook(raw:string,signature:string,secret:string){if(!secret)return false;const expected=createHmac("sha256",secret).update(raw).digest("hex");try{return timingSafeEqual(Buffer.from(signature,"hex"),Buffer.from(expected,"hex"))}catch{return false}}export function csvSafe(value:string){return/^[=+\-@	]/.test(value)?`'${value}`:value}
