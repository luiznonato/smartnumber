import{describe,it,expect}from"vitest";import{csvSafe,hashPassword,issueSession,verifyPassword,verifyWebhook}from"./auth.js";import{consumeQuota,SandboxBillingProvider}from"./quota.js";describe("security and SaaS",()=>{it("hashes Argon2id passwords",async()=>{const h=await hashPassword("uma-senha-bem-longa");expect(await verifyPassword(h,"uma-senha-bem-longa")).toBe(true);expect(h).toContain("argon2id")});it("does not store session token",()=>{const s=issueSession();expect(s.token).not.toBe(s.tokenHash)});it("rejects unsigned billing",()=>expect(verifyWebhook("x","bad","")).toBe(false));it("prevents CSV formulas",()=>expect(csvSafe("=1+1")).toBe("'=1+1"));it("consumes quotas idempotently",()=>{const c={used:0,limit:1,seen:new Set<string>()};consumeQuota(c,1,"a");consumeQuota(c,1,"a");expect(c.used).toBe(1);expect(()=>consumeQuota(c,1,"b")).toThrow()});it("fails closed in billing sandbox",async()=>await expect(new SandboxBillingProvider().createHostedCheckout("u","p")).rejects.toThrow())});